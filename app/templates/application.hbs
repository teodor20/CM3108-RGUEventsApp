<div class="container">
  <div class="menu">
    {{#link-to "index"}}
      <h1>
        <em>RGU Events Calendar</em>
      </h1>
    {{/link-to}}
    <div class="links">
      {{#link-to "events" class="menu-contact"}}
        What's On
      {{/link-to}}
      {{#link-to "about" class="menu-about"}}
        About
      {{/link-to}}
      {{#link-to "users" class="menu-users"}}
        Users
      {{/link-to}}
      {{#link-to "profile" class="menu-profile"}}
        My Profile
      {{/link-to}}
       <button class="btn btn-secondary" {{action 'goToLoginPage' }}>Logout</button>
    </div>
  </div>
  <div class="body">
    {{outlet}}
  </div>
  <div class="footer">
    &copy;2019 RGU Team Old Boys.
  </div>
</div>